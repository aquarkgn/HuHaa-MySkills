# HuHaa-MySkills 编辑器规则

## 项目概述

HuHaa-MySkills 是一个本地 AI 技能聚合中枢。它从 Hermes、Claude Code、Cursor、Codex、Obsidian 和各项目根目录中聚合技能、插件、MCP 配置和部署 RUNBOOK，提供统一的浏览器界面进行搜索和管理。

## 核心职责

1. **多源技能聚合** - 扫描并统一展示来自多个工具的技能和配置
2. **浏览器界面** - 提供 http://localhost:11520 的搜索和管理界面
3. **编辑器集成** - 支持在 20+ 个编辑器中同步和打开技能
4. **热更新支持** - 文件变更时自动刷新界面

## 技术栈

- **Runtime**: Node.js 20+
- **Backend**: Fastify 5 + Chokidar
- **Frontend**: Vue 3 + Vite
- **扫描**: fast-glob + 自定义适配器
- **UI 库**: Vuetify

## 主要命令

```bash
# 启动服务
npm start

# 扫描技能（仅输出）
npm run scan

# 同步到编辑器
npm run sync

# 开发模式
npm dev
```

## 编辑器同步

`npm run sync` 命令支持：

- **Cursor** - 同步 `.cursorrules` 规则文件
- **VS Code** - 同步为参考文件
- **Windsurf** - 同步 `.cursorrules`
- **Zed** - 同步 `.cursorrules`
- **JetBrains IDEs** - 同步规则文件
- **Vim/Neovim** - 同步为 markdown 文件
- **Helix** - 同步为 rules.md
- **Emacs** - 同步为 huhaa-rules.md
- 以及其他 20+ 个编辑器

## 目录结构

```
HuHaa-MySkills/
├── packages/
│   ├── scanner/      # 多源技能扫描器
│   ├── server/       # Fastify HTTP 服务
│   └── web/          # Vue 3 前端界面
├── scripts/
│   ├── sync-skills.sh    # 编辑器同步脚本
│   └── build-web.mjs     # 前端构建
├── config/
│   └── sources.yaml  # 扫描源配置
└── docs/
    └── SYNC-SKILLS.md    # 同步使用文档
```

## 快速开始

### 本地开发

```bash
git clone https://github.com/aquarkgn/HuHaa-MySkills.git
cd HuHaa-MySkills
npm install
npm start
```

### 编辑器同步

```bash
npm run sync
# 选择要同步的编辑器（支持多选）
```

### 一键安装

```bash
curl -fsSL https://raw.githubusercontent.com/aquarkgn/HuHaa-MySkills/main/install-and-sync.sh | bash
```

## 扫描的技能源

- `~/.hermes/skills/**/*.md` - Hermes 技能
- `~/.claude/skills/**/*.md` - Claude Code 技能
- 项目根目录 `.cursorrules` - Cursor 规则
- 项目根目录 `CLAUDE.md` - Claude 规则
- 项目根目录 `AGENTS.md` - Agent 规则
- MCP 配置文件 - 所有配置的 MCP 服务和工具

## 常见问题

### Q: 如何添加自己的技能？

A: 在 `~/.hermes/skills/` 或 `~/.claude/skills/` 目录中创建新文件夹，包含 SKILL.md 文件。

### Q: 同步到编辑器后如何验证？

A: 检查编辑器配置目录中是否有相应的规则文件被创建或更新。

### Q: 支持 Windows 吗？

A: 支持 Windows WSL。在 PowerShell 中：
```powershell
wsl bash -c "npm run sync"
```

## 相关链接

- [GitHub 仓库](https://github.com/aquarkgn/HuHaa-MySkills)
- [使用文档](./docs/SYNC-SKILLS.md)
- [项目规划](./docs/PLAN.md)
- [发布规范](./docs/RULES.md)

---

**最后更新**: 2026/06/22
