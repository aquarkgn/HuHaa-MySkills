# 历史推广资料说明

`docs/调研报告/` 内的深度研究和原镜头绑定表保留为调研证据，不删除、不覆盖。

从 SkillsHelper 品牌启用后，以下内容不再是对外发布的唯一口径：固定的版本号、release 数量、Star/Fork 数量、本机扫描数量，以及依赖特定开发环境的端口或已安装应用。发布时请以 `../assets/shot-catalog.yaml` 的 evidence 字段和录制当日验证结果为准。
